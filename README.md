# YEG祭2026 会場案内（本番用）

AI生成画像は使用していません。ユーザー提供の会場マップ画像を `index.html` 内へ埋め込み、番号①〜⑳を参考画像の位置・大きさに合わせて調整しています。

## GitHub Pages
このフォルダ内の `index.html`、`.nojekyll`、`vercel.json`、`README.md` をリポジトリ最上位へアップロードしてください。
